HOST=127.0.0.1
PORT=7676
# Owner password generated by `devspace init` in ~/.devspace/auth.json. Set this
# only for non-interactive or fully env-driven deployments.
# DEVSPACE_OAUTH_OWNER_TOKEN=change-me-to-a-long-random-secret
# Optional OAuth tuning.
# DEVSPACE_OAUTH_ACCESS_TOKEN_TTL_SECONDS=3600
# DEVSPACE_OAUTH_REFRESH_TOKEN_TTL_SECONDS=2592000
# DEVSPACE_OAUTH_SCOPES=devspace
# DEVSPACE_OAUTH_ALLOWED_REDIRECT_HOSTS=chatgpt.com,localhost,127.0.0.1
# Legacy shorthand: every listed root is read-write.
DEVSPACE_ALLOWED_ROOTS=/home/waishnav/personal,/home/waishnav/work
# Policy form for aliases and read-only roots. Do not set both root variables.
# DEVSPACE_ROOTS=[{"path":"/srv/pool/projects","aliases":["/home/waishnav/projects"],"access":"read-write"},{"path":"/etc","access":"read-only"}]
# For temporary tunnels, prefer setting this per run. DevSpace derives the
# inbound Host allowlist from this URL.
# DEVSPACE_PUBLIC_BASE_URL=https://your-public-host.example.com
# Advanced escape hatch. `*` disables Host header allowlist protection.
# DEVSPACE_ALLOWED_HOSTS=localhost,127.0.0.1,your-public-host.example.com
DEVSPACE_TOOL_MODE=full
DEVSPACE_TOOL_NAMING=legacy
# standard | trusted-owner. trusted-owner keeps mutating tools writable while
# advertising them as non-destructive and closed-world to MCP hosts.
# DEVSPACE_ANNOTATION_PROFILE=standard
# off | changes | full. Defaults to changes.
# changes creates one aggregate review widget via review_changes instead of per-tool iframes.
DEVSPACE_WIDGETS=changes
DEVSPACE_LOG_LEVEL=info
DEVSPACE_LOG_FORMAT=json
DEVSPACE_LOG_REQUESTS=1
DEVSPACE_LOG_ASSETS=0
DEVSPACE_LOG_TOOL_CALLS=1
DEVSPACE_LOG_SHELL_COMMANDS=0
# DEVSPACE_TRUST_PROXY=1
# DEVSPACE_STATE_DIR=/home/waishnav/.local/share/devspace
# DEVSPACE_WORKTREE_ROOT=/home/waishnav/.devspace/worktrees
# service | login. Login mode discovers Bun, mise, OpenCode, and other user tools.
# DEVSPACE_SHELL_MODE=login
# DEVSPACE_SHELL_PATH=/bin/bash
# Exact DevSpace-owned secret names to remove from child shells and terminals.
# DEVSPACE_INFRA_SECRET_NAMES=DEVSPACE_ASGARD_NODE_TOKEN
# DEVSPACE_TERMINAL_RUNTIME_DIR=/run/user/1000/devspace
# DEVSPACE_TERMINAL_MAX_PER_WORKSPACE=4
# DEVSPACE_TERMINAL_MAX_TOTAL=12
# DEVSPACE_TERMINAL_IDLE_TTL_SECONDS=28800
# DEVSPACE_TERMINAL_USER_SYSTEMD=1
# DEVSPACE_MAINTENANCE_INTERVAL_SECONDS=3600
# DEVSPACE_CLOSED_SESSION_TTL_SECONDS=604800
# DEVSPACE_CHECKOUT_IDLE_TTL_SECONDS=2592000
# DEVSPACE_ISOLATED_IDLE_TTL_SECONDS=604800
# Optional global DevSpace instructions loaded before project AGENTS.md.
# Defaults to ~/.devspace/AGENTS.md and is ignored when the file is absent.
# DEVSPACE_GLOBAL_INSTRUCTIONS_FILE=/home/waishnav/.devspace/AGENTS.md
# Skills are enabled by default. Set DEVSPACE_SKILLS=0 to hide them.
# DEVSPACE_SKILLS=0
# DEVSPACE_AGENT_DIR=/home/waishnav/.codex
# DEVSPACE_SKILL_PATHS=/home/waishnav/.codex/skills,/home/waishnav/.claude/skills
